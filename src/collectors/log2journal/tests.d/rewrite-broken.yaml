---
# Test rewrite with uppercase keys (journal_key_characters_map uppercases captured keys)
pattern: "(?P<level>\\w+)\\s+(?P<message>.*)"

rewrite:
  - key: LEVEL
    match: "ERROR"
    value: "CRITICAL"