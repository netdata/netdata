[package]
name = "wal"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
crc32fast = { workspace = true }
file-registry = { workspace = true }
journal-common = { workspace = true }
lz4_flex = { workspace = true }
nix = { workspace = true, features = ["fs"] }
serde = { workspace = true, features = ["derive"] }
thiserror = { workspace = true }
tracing = { workspace = true }
uuid = { workspace = true, features = ["serde"] }

[dev-dependencies]
tempfile = { workspace = true }
# Transitional: tests build identities via the content-plane codec. 2b-cleanup
# commit 2 rewrites them to opaque part_keys and removes this dev-dep.
otel-logs-identity = { workspace = true }
