# Default scheduler definition so scripts.d exposes the built-in worker pool in dyncfg.
jobs:
  - name: default
    workers: 50
    queue_size: 128
    logging:
      enabled: true
      otlp:
        endpoint: 127.0.0.1:4317
        tls: false
        timeout: 5s
