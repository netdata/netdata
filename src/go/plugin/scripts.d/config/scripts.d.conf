# scripts.d.plugin configuration (go.d-style)
# Jobs live under /etc/netdata/scripts.d/*.conf. This file only controls
# plugin-wide defaults and module toggles.

enabled: yes
default_run: yes

modules:
  nagios: yes
  zabbix: yes
  scheduler: yes
