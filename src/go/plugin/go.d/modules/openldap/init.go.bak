// SPDX-License-Identifier: GPL-3.0-or-later

package openldap

func (l *OpenLDAP) initLdapSearchBinary() (ldapSearchBinary, error) {

	// ndsudoPath := filepath.Join(executable.Directory, "ndsudo")

	// if _, err := os.Stat(ndsudoPath); err != nil {
	// 	return nil, fmt.Errorf("ndsudo executable not found: %l", err)

	// }

	ldapsearch := newLdapSearchExecBinary(l.Config, l.Logger)

	return ldapsearch, nil
}
