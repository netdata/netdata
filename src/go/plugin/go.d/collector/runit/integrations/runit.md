<!--startmeta
custom_edit_url: "https://github.com/netdata/netdata/edit/master/src/go/plugin/go.d/collector/runit/README.md"
meta_yaml: "https://github.com/netdata/netdata/edit/master/src/go/plugin/go.d/collector/runit/metadata.yaml"
sidebar_label: "Runit services"
learn_status: "Published"
learn_rel_path: "Collecting Metrics/Processes and System Services"
most_popular: False
keywords: ['runit', 'service']
message: "DO NOT EDIT THIS FILE DIRECTLY, IT IS GENERATED BY THE COLLECTOR'S metadata.yaml FILE"
endmeta-->

# Runit services


<img src="https://netdata.cloud/img/runit.png" width="150"/>


Plugin: go.d.plugin
Module: runit

<img src="https://img.shields.io/badge/maintained%20by-Community-%2300ab44" />

## Overview

This collector monitors the state of Runit services.
It relies on the [`sv`](https://smarden.org/runit/sv.8) CLI tool but avoids directly executing the binary.
Instead, it utilizes `ndsudo`, a Netdata helper specifically designed to run privileged commands securely within the Netdata environment.
This approach eliminates the need to use `sudo`, improving security and potentially simplifying permission management.

Executed commands:
-  `sv status <service_dir>/*`




This collector supports collecting metrics from multiple instances of this integration, including from remote instances.


### Default Behavior

#### Auto-Detection

By default, it detects services running in
`$SVDIR` or (if `$SVDIR` is empty) /service directory.

#### Limits

The default configuration for this integration does not impose any limits on data collection.

#### Performance Impact

The default configuration for this integration is not expected to impose a significant performance impact on the system.

## Metrics

Metrics grouped by *scope*.

The scope defines the instance that the metric belongs to. An instance is uniquely identified by a set of labels.



### Per runit instance

These metrics refer to the entire monitored application.

This scope has no labels.

Metrics:

| Metric | Dimensions | Unit |
|:------|:----------|:----|
| runit.services | running, not_running | services |

### Per service

These metrics refer to the runit service.

Labels:

| Label      | Description     |
|:-----------|:----------------|
| service_name | runit service name |

Metrics:

| Metric | Dimensions | Unit |
|:------|:----------|:----|
| runit.service_state | running, down, failed, starting, stopping, paused | state |
| runit.service_state_duration | down, running | seconds |



## Alerts


The following alerts are available:

| Alert name  | On metric | Description |
|:------------|:----------|:------------|
| [ runit_service_failed_state ](https://github.com/netdata/netdata/blob/master/src/health/health.d/runit.conf) | runit.service_state | runit service in the failed state (should be running but is not) |
| [ runit_service_undesired_running ](https://github.com/netdata/netdata/blob/master/src/health/health.d/runit.conf) | runit.service_state | runit service is running but should be down (normally down) |
| [ runit_service_paused_state ](https://github.com/netdata/netdata/blob/master/src/health/health.d/runit.conf) | runit.service_state | runit service is paused |
| [ runit_service_stuck_transitioning ](https://github.com/netdata/netdata/blob/master/src/health/health.d/runit.conf) | runit.service_state | runit service stuck in transitioning state (starting or stopping) |


## Setup


You can configure the **runit** collector in two ways:

| Method                | Best for                                                                                 | How to                                                                                                                                 |
|-----------------------|------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------|
| [**UI**](#via-ui)     | Fast setup without editing files                                                         | Go to **Nodes → Configure this node → Collectors → Jobs**, search for **runit**, then click **+** to add a job. |
| [**File**](#via-file) | If you prefer configuring via file, or need to automate deployments (e.g., with Ansible) | Edit `go.d/runit.conf` and add a job.                                                                        |

:::important

UI configuration requires paid Netdata Cloud plan.

:::


### Prerequisites

#### Install sv (included in runit package)

If you want this collector then you already have runit package installed.
But if you'll run Netdata in a Docker then you need to also install runit in Netdata container.

#### Docker volume mounts for runit service directories

Runit services make heavy use of symlinks (e.g. `/service/sshd` → `/etc/sv/sshd`,
`run` and `finish` scripts inside service dirs, `supervise/` state directories, etc.).

When running Netdata in Docker, you must bind-mount the runit service directories
at their **original host paths**, not under a host-prefix mount point.
Otherwise `sv status` will fail to resolve symlinks and will not report service states.

For example, if the host uses `/service` as the service directory with actual
service definitions in `/etc/sv/`:

```yaml
volumes:
  - /service:/service:ro
  - /etc/sv:/etc/sv:ro
```

Do **not** mount them under a prefix like `/host/service` — the symlink targets
inside the container must match the original host paths.

### Configuration

#### Options

The following options can be defined globally: update_every, autodetection_retry.


<details open><summary>Config options</summary>



| Option | Description | Default | Required |
|:-----|:------------|:--------|:---------:|
| update_every | Data collection frequency. | 1 | no |
| autodetection_retry | Recheck interval in seconds. Zero means no recheck will be scheduled. | 60 | no |
| dir | Directory with runit services. If environment variable $SVDIR is set to non-empty value then it will be used as default instead of /service. | /service | no |


</details>


#### via UI

Configure the **runit** collector from the Netdata web interface:

1. Go to **Nodes**.
2. Select the node **where you want the runit data-collection job to run** and click the :gear: (**Configure this node**). That node will run the data collection.
3. The **Collectors → Jobs** view opens by default.
4. In the Search box, type _runit_ (or scroll the list) to locate the **runit** collector.
5. Click the **+** next to the **runit** collector to add a new job.
6. Fill in the job fields, then click **Test** to verify the configuration and **Submit** to save.
    - **Test** runs the job with the provided settings and shows whether data can be collected.
    - If it fails, an error message appears with details (for example, connection refused, timeout, or command execution errors), so you can adjust and retest.


#### via File

The configuration file name for this integration is `go.d/runit.conf`.

The file format is YAML. Generally, the structure is:

```yaml
update_every: 1
autodetection_retry: 0
jobs:
  - name: some_name1
  - name: some_name2
```
You can edit the configuration file using the [`edit-config`](https://github.com/netdata/netdata/blob/master/docs/netdata-agent/configuration/README.md#edit-configuration-files) script from the
Netdata [config directory](https://github.com/netdata/netdata/blob/master/docs/netdata-agent/configuration/README.md#locate-your-config-directory).

```bash
cd /etc/netdata 2>/dev/null || cd /opt/netdata/etc/netdata
sudo ./edit-config go.d/runit.conf
```

##### Examples

###### System services in non-default dir

Set custom directory for system services.

<details open><summary>Config</summary>

```yaml
jobs:
  - name: system
    dir: /etc/service
```
</details>

###### Some user also run services in addition to system ones

> **Note**: When you define multiple jobs, their names must be unique.

Add directory with user services.

<details open><summary>Config</summary>

```yaml
jobs:
  - name: system

  - name: someone
    dir: /home/someone/service
```
</details>



## Troubleshooting

### Debug Mode

**Important**: Debug mode is not supported for data collection jobs created via the UI using the Dyncfg feature.

To troubleshoot issues with the `runit` collector, run the `go.d.plugin` with the debug option enabled. The output
should give you clues as to why the collector isn't working.

- Navigate to the `plugins.d` directory, usually at `/usr/libexec/netdata/plugins.d/`. If that's not the case on
  your system, open `netdata.conf` and look for the `plugins` setting under `[directories]`.

  ```bash
  cd /usr/libexec/netdata/plugins.d/
  ```

- Switch to the `netdata` user.

  ```bash
  sudo -u netdata -s
  ```

- Run the `go.d.plugin` to debug the collector:

  ```bash
  ./go.d.plugin -d -m runit
  ```

  To debug a specific job:

  ```bash
  ./go.d.plugin -d -m runit -j jobName
  ```

### Getting Logs

If you're encountering problems with the `runit` collector, follow these steps to retrieve logs and identify potential issues:

- **Run the command** specific to your system (systemd, non-systemd, or Docker container).
- **Examine the output** for any warnings or error messages that might indicate issues.  These messages should provide clues about the root cause of the problem.

#### System with systemd

Use the following command to view logs generated since the last Netdata service restart:

```bash
journalctl _SYSTEMD_INVOCATION_ID="$(systemctl show --value --property=InvocationID netdata)" --namespace=netdata --grep runit
```

#### System without systemd

Locate the collector log file, typically at `/var/log/netdata/collector.log`, and use `grep` to filter for collector's name:

```bash
grep runit /var/log/netdata/collector.log
```

**Note**: This method shows logs from all restarts. Focus on the **latest entries** for troubleshooting current issues.

#### Docker Container

If your Netdata runs in a Docker container named "netdata" (replace if different), use this command:

```bash
docker logs netdata 2>&1 | grep runit
```
