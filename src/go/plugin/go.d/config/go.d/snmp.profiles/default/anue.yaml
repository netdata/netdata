extends:
  - _system-base.yaml
  - _std-if-mib.yaml

metadata:
  device:
    fields:
      vendor:
        value: Anue

# TODO, there exists a MIB for Anue, explore.

selector:
  - sysobjectid:
      include:
        - 1.3.6.1.4.1.32620.1.*
