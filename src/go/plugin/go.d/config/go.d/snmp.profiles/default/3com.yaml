extends:
  - _system-base.yaml
  - _std-if-mib.yaml

metadata:
  device:
    fields:
      vendor:
        value: "3Com-Huawei"

selector:
  - sysobjectid:
      include:
        - 1.3.6.1.4.1.43.1.* # 3Com products
