extends:
  - _system-base.yaml

metadata:
  device:
    fields:
      vendor:
        value: "Tripp Lite"

selector:
  - sysobjectid:
      include:
        - 1.3.6.1.4.1.850.1.* # tlpProducts: all tripplite products
