extends:
  - _system-base.yaml
  - _std-if-mib.yaml
  - _std-ucd-mib.yaml

metadata:
  device:
    fields:
      vendor:
        value: "Ruckus"

selector:
  - sysobjectid:
      include:
        - 1.3.6.1.4.1.25053.3.*
