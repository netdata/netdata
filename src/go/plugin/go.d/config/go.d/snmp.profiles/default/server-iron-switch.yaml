extends:
  - _system-base.yaml
  - _std-host-resources-mib-feat-base.yaml
  - _std-if-mib.yaml

metadata:
  device:
    fields:
      vendor:
        value: "Server Iron"
      type:
        value: "Load Balancer"

sysobjectid:
  - 1.3.6.1.4.1.1991.1.3.3.2  # Server Iron XL switches
