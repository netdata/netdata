extends:
  - _system-base.yaml
  - _std-if-mib.yaml
  - _std-host-resources-mib-feat-base.yaml

metadata:
  device:
    fields:
      vendor:
        value: "Riverbed"

selector:
  - sysobjectid:
      include:
        - 1.3.6.1.4.1.17163.1.*
