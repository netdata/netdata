# netdata go.d.plugin configuration for Xquik
#
# This file is in YAML format.

# jobs:
#   - name: x-account
#     user: netdata
#     api_key: ${env:XQUIK_API_KEY}
#
#     # Xquik REST API base URL. Production endpoints must use HTTPS.
#     url: https://xquik.com/api/v1
#
#     # Profile reads are metered. The minimum collection interval is 60 seconds.
#     update_every: 300
#     timeout: 30
