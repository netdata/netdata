# StatsD receiver jobs. No listener is enabled by default.
#
# Every listed listener must bind for the job to start.
#
# Profiles are YAML files selected by name from /etc/netdata/statsd/statsd.profiles/.

#jobs:
#  - name: local
#    listeners:
#      - protocol: both # udp, tcp or both (the default)
#        address: 127.0.0.1:8126
#    profiles: []
#    max_series: 1000
#    metric_idle_timeout: 30m
#    max_tcp_connections: 64
