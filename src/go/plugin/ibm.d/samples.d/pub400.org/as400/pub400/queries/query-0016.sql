SELECT 
			JOB_QUEUE_LIBRARY || '/' || JOB_QUEUE_NAME as QUEUE_NAME,
			NUMBER_OF_JOBS
		FROM QSYS2.JOB_QUEUE_INFO
		WHERE JOB_QUEUE_STATUS = 'RELEASED'
		ORDER BY NUMBER_OF_JOBS DESC FETCH FIRST 200 ROWS ONLY