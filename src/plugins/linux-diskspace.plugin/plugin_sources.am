# SPDX-License-Identifier: GPL-3.0-or-later

# the list of files for this plugin

# We don't build static libraries
# because LTO does not always work with them (eg alpine)

diskspace_dir = $(abs_top_srcdir)/src/plugins/linux-diskspace.plugin

diskspace_SOURCES = \
    $(diskspace_dir)/plugin_diskspace.h \
    $(diskspace_dir)/plugin_diskspace.c \
    $(NULL)
