# SPDX-License-Identifier: GPL-3.0-or-later

# the list of files for this plugin

# We don't build static libraries
# because LTO does not always work with them (eg alpine)

checks_dir = $(top_srcdir)/src/plugins/checks.plugin

checks_SOURCES = \
    $(checks_dir)/plugin_checks.c \
    $(checks_dir)/plugin_checks.h \
    $(NULL)
