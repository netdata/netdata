# SPDX-License-Identifier: GPL-3.0-or-later

# the list of files for this plugin

# We don't build static libraries
# because LTO does not always work with them (eg alpine)

nfacct_dir = $(top_srcdir)/src/plugins/linux-nfacct.plugin

nfacct_SOURCES = \
	$(nfacct_dir)/plugin_nfacct.c \
	$(nfacct_dir)/plugin_nfacct.h \
    $(NULL)
