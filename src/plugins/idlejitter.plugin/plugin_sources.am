# SPDX-License-Identifier: GPL-3.0-or-later

# the list of files for this plugin

# We don't build static libraries
# because LTO does not always work with them (eg alpine)

idlejitter_dir = $(top_srcdir)/src/plugins/idlejitter.plugin

idlejitter_SOURCES = \
	$(idlejitter_dir)/plugin_idlejitter.c \
	$(idlejitter_dir)/plugin_idlejitter.h \
    $(NULL)
