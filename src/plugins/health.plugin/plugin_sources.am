# SPDX-License-Identifier: GPL-3.0-or-later

# the list of files for this plugin

# We don't build static libraries
# because LTO does not always work with them (eg alpine)

health_dir = $(top_srcdir)/src/plugins/health.plugin

health_SOURCES = \
	$(health_dir)/health.c \
	$(health_dir)/health.h \
	$(health_dir)/health_config.c \
	$(health_dir)/health_json.c \
	$(health_dir)/health_log.c \
    $(NULL)
