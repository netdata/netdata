# SPDX-License-Identifier: GPL-3.0-or-later
#
# Generated by src/go/plugin/go.d/collector/redfish/chartgen.
# Do not edit directly; update the executable Redfish registry or approved alert policy.

 template: redfish_aggregate_conditions
       on: redfish.aggregate.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:rollup_owner_name} Condition Counts
     info: Redfish ${label:rollup_owner_name} reports a non-healthy state for Condition Counts
       to: sysadmin

 template: redfish_aggregate_failure_predicted
       on: redfish.aggregate.failure_predicted
    class: Errors
     type: System
component: Hardware
     calc: $predicted
    units: components
    every: 10s
     crit: $predicted > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:rollup_owner_name} Failure Prediction Counts
     info: Redfish ${label:rollup_owner_name} reports a non-healthy state for Failure Prediction Counts
       to: sysadmin

 template: redfish_aggregate_health
       on: redfish.aggregate.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: components
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:rollup_owner_name} Health Counts
     info: Redfish ${label:rollup_owner_name} reports a non-healthy state for Health Counts
       to: sysadmin

 template: redfish_aggregate_health_rollup
       on: redfish.aggregate.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: components
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:rollup_owner_name} Health Rollup Counts
     info: Redfish ${label:rollup_owner_name} reports a non-healthy state for Health Rollup Counts
       to: sysadmin

 template: redfish_aggregate_reading_alarm
       on: redfish.aggregate.reading_alarm
    class: Errors
     type: System
component: Hardware
     calc: $warning + $cap + $alarm + $critical + $emergency + $fault
    units: components
    every: 10s
     warn: $warning > 0 OR $cap > 0 OR $alarm > 0
     crit: $critical > 0 OR $emergency > 0 OR $fault > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:rollup_owner_name} Reading Alarm Counts
     info: Redfish ${label:rollup_owner_name} reports a non-healthy state for Reading Alarm Counts
       to: sysadmin

 template: redfish_assembly_conditions
       on: redfish.assembly.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Assembly Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Assembly Conditions
       to: sysadmin

 template: redfish_assembly_health
       on: redfish.assembly.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Assembly Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Assembly Health
       to: sysadmin

 template: redfish_assembly_health_rollup
       on: redfish.assembly.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Assembly Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Assembly Health Rollup
       to: sysadmin

 template: redfish_battery_conditions
       on: redfish.battery.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Battery Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Battery Conditions
       to: sysadmin

 template: redfish_battery_health
       on: redfish.battery.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Battery Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Battery Health
       to: sysadmin

 template: redfish_battery_health_rollup
       on: redfish.battery.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Battery Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Battery Health Rollup
       to: sysadmin

 template: redfish_chassis_conditions
       on: redfish.chassis.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Chassis Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Chassis Conditions
       to: sysadmin

 template: redfish_chassis_health
       on: redfish.chassis.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Chassis Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Chassis Health
       to: sysadmin

 template: redfish_chassis_health_rollup
       on: redfish.chassis.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Chassis Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Chassis Health Rollup
       to: sysadmin

 template: redfish_chassis_intrusion_state
       on: redfish.chassis.intrusion_state
    class: Errors
     type: System
component: Hardware
     calc: $hardware_intrusion + $tampering_detected
    units: state
    every: 10s
     crit: $hardware_intrusion > 0 OR $tampering_detected > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Chassis Intrusion State
     info: Redfish ${label:resource_name} reports a non-healthy state for Chassis Intrusion State
       to: sysadmin

 template: redfish_collection_logs_admission
       on: redfish.collection.logs_admission
    class: Errors
     type: System
component: Redfish
     calc: $over_limit + $duplicate_owner
    units: state
    every: 10s
     warn: $over_limit > 0 OR $duplicate_owner > 0
    delay: up 5m down 5m multiplier 1.5 max 1h
  summary: Redfish endpoint ${label:endpoint_job} Redfish Logs Admission
     info: Redfish endpoint ${label:endpoint_job} reports a non-healthy state for Redfish Logs Admission
       to: sysadmin

 template: redfish_collection_selected_system
       on: redfish.collection.selected_system
    class: Availability
     type: System
component: Redfish
     calc: $unreadable + $absent
    units: state
    every: 10s
     warn: $unreadable > 0
     crit: $absent > 0
    delay: up 5m down 5m multiplier 1.5 max 1h
  summary: Redfish endpoint ${label:endpoint_job} Redfish Selected System State
     info: Redfish endpoint ${label:endpoint_job} reports a non-healthy state for Redfish Selected System State
       to: sysadmin

 template: redfish_collection_status
       on: redfish.collection.status
    class: Availability
     type: System
component: Redfish
     calc: $partial + $unavailable
    units: state
    every: 10s
     warn: $partial > 0
     crit: $unavailable > 0
    delay: up 5m down 5m multiplier 1.5 max 1h
  summary: Redfish endpoint ${label:endpoint_job} Redfish Collection Status
     info: Redfish endpoint ${label:endpoint_job} reports a non-healthy state for Redfish Collection Status
       to: sysadmin

 template: redfish_control_conditions
       on: redfish.control.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Control Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Control Conditions
       to: sysadmin

 template: redfish_control_health
       on: redfish.control.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Control Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Control Health
       to: sysadmin

 template: redfish_control_health_rollup
       on: redfish.control.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Control Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Control Health Rollup
       to: sysadmin

 template: redfish_coolant_connector_conditions
       on: redfish.coolant_connector.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Coolant Connector Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Coolant Connector Conditions
       to: sysadmin

 template: redfish_coolant_connector_health
       on: redfish.coolant_connector.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Coolant Connector Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Coolant Connector Health
       to: sysadmin

 template: redfish_coolant_connector_health_rollup
       on: redfish.coolant_connector.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Coolant Connector Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Coolant Connector Health Rollup
       to: sysadmin

 template: redfish_drive_conditions
       on: redfish.drive.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Drive Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Drive Conditions
       to: sysadmin

 template: redfish_drive_failure_predicted
       on: redfish.drive.failure_predicted
    class: Errors
     type: System
component: Hardware
     calc: $predicted
    units: state
    every: 10s
     crit: $predicted > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Drive Failure Prediction
     info: Redfish ${label:resource_name} reports a non-healthy state for Drive Failure Prediction
       to: sysadmin

 template: redfish_drive_health
       on: redfish.drive.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Drive Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Drive Health
       to: sysadmin

 template: redfish_drive_health_rollup
       on: redfish.drive.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Drive Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Drive Health Rollup
       to: sysadmin

 template: redfish_drive_status_indicator
       on: redfish.drive.status_indicator
    class: Errors
     type: System
component: Hardware
     calc: $rebuild + $predictive_failure_analysis + $fail + $in_a_critical_array + $in_a_failed_array
    units: state
    every: 10s
     warn: $rebuild > 0 OR $predictive_failure_analysis > 0
     crit: $fail > 0 OR $in_a_critical_array > 0 OR $in_a_failed_array > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Drive Status Indicator
     info: Redfish ${label:resource_name} reports a non-healthy state for Drive Status Indicator
       to: sysadmin

 template: redfish_ethernet_interface_conditions
       on: redfish.ethernet_interface.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Ethernet Interface Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Ethernet Interface Conditions
       to: sysadmin

 template: redfish_ethernet_interface_health
       on: redfish.ethernet_interface.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Ethernet Interface Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Ethernet Interface Health
       to: sysadmin

 template: redfish_ethernet_interface_health_rollup
       on: redfish.ethernet_interface.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Ethernet Interface Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Ethernet Interface Health Rollup
       to: sysadmin

 template: redfish_fan_conditions
       on: redfish.fan.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Fan Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Fan Conditions
       to: sysadmin

 template: redfish_fan_health
       on: redfish.fan.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Fan Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Fan Health
       to: sysadmin

 template: redfish_fan_health_rollup
       on: redfish.fan.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Fan Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Fan Health Rollup
       to: sysadmin

 template: redfish_filter_conditions
       on: redfish.filter.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Filter Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Filter Conditions
       to: sysadmin

 template: redfish_filter_health
       on: redfish.filter.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Filter Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Filter Health
       to: sysadmin

 template: redfish_filter_health_rollup
       on: redfish.filter.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Filter Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Filter Health Rollup
       to: sysadmin

 template: redfish_firmware_conditions
       on: redfish.firmware.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Firmware Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Firmware Conditions
       to: sysadmin

 template: redfish_firmware_health
       on: redfish.firmware.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Firmware Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Firmware Health
       to: sysadmin

 template: redfish_firmware_health_rollup
       on: redfish.firmware.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Firmware Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Firmware Health Rollup
       to: sysadmin

 template: redfish_heater_conditions
       on: redfish.heater.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Heater Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Heater Conditions
       to: sysadmin

 template: redfish_heater_health
       on: redfish.heater.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Heater Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Heater Health
       to: sysadmin

 template: redfish_heater_health_rollup
       on: redfish.heater.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Heater Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Heater Health Rollup
       to: sysadmin

 template: redfish_leak_detection_conditions
       on: redfish.leak_detection.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Leak Detection Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Leak Detection Conditions
       to: sysadmin

 template: redfish_leak_detection_health
       on: redfish.leak_detection.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Leak Detection Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Leak Detection Health
       to: sysadmin

 template: redfish_leak_detection_health_rollup
       on: redfish.leak_detection.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Leak Detection Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Leak Detection Health Rollup
       to: sysadmin

 template: redfish_leak_detector_conditions
       on: redfish.leak_detector.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Leak Detector Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Leak Detector Conditions
       to: sysadmin

 template: redfish_leak_detector_detector_state
       on: redfish.leak_detector.detector_state
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Leak Detector State
     info: Redfish ${label:resource_name} reports a non-healthy state for Leak Detector State
       to: sysadmin

 template: redfish_leak_detector_health
       on: redfish.leak_detector.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Leak Detector Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Leak Detector Health
       to: sysadmin

 template: redfish_leak_detector_health_rollup
       on: redfish.leak_detector.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Leak Detector Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Leak Detector Health Rollup
       to: sysadmin

 template: redfish_leak_detector_group_conditions
       on: redfish.leak_detector_group.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Leak Detector Group Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Leak Detector Group Conditions
       to: sysadmin

 template: redfish_leak_detector_group_health
       on: redfish.leak_detector_group.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Leak Detector Group Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Leak Detector Group Health
       to: sysadmin

 template: redfish_leak_detector_group_health_rollup
       on: redfish.leak_detector_group.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Leak Detector Group Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Leak Detector Group Health Rollup
       to: sysadmin

 template: redfish_log_backend_pipeline
       on: redfish.log_backend.pipeline
    class: Errors
     type: System
component: Redfish
     calc: $write_failed
    units: entries/s
    every: 10s
     warn: $this > 0
    delay: up 5m down 5m multiplier 1.5 max 1h
  summary: Redfish backend ${label:backend_name} Redfish Log Backend Pipeline
     info: Redfish backend ${label:backend_name} reports a non-healthy state for Redfish Log Backend Pipeline
       to: sysadmin

 template: redfish_log_backend_state
       on: redfish.log_backend.state
    class: Availability
     type: System
component: Redfish
     calc: $unavailable
    units: state
    every: 10s
     crit: $unavailable > 0
    delay: up 5m down 5m multiplier 1.5 max 1h
  summary: Redfish backend ${label:backend_name} Redfish Log Backend State
     info: Redfish backend ${label:backend_name} reports a non-healthy state for Redfish Log Backend State
       to: sysadmin

 template: redfish_log_service_conditions
       on: redfish.log_service.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Log Service Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Log Service Conditions
       to: sysadmin

 template: redfish_log_service_health
       on: redfish.log_service.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Log Service Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Log Service Health
       to: sysadmin

 template: redfish_log_service_health_rollup
       on: redfish.log_service.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Log Service Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Log Service Health Rollup
       to: sysadmin

 template: redfish_log_service_ingestion_state_critical
       on: redfish.log_service.ingestion_state
    class: Errors
     type: System
component: Redfish
     calc: $blocked_source
    units: state
    every: 10s
     crit: $this > 0
    delay: up 30m down 5m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} log ingestion persistently blocked
     info: Redfish ${label:resource_name} log ingestion has remained blocked at its source for at least thirty minutes
       to: sysadmin

 template: redfish_log_service_ingestion_state_warning
       on: redfish.log_service.ingestion_state
    class: Errors
     type: System
component: Redfish
     calc: $blocked_source
    units: state
    every: 10s
     warn: $this > 0
    delay: up 5m down 5m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} log ingestion blocked
     info: Redfish ${label:resource_name} log ingestion has remained blocked at its source for at least five minutes
       to: sysadmin

 template: redfish_log_service_overflow_state
       on: redfish.log_service.overflow_state
    class: Errors
     type: System
component: Hardware
     calc: $overflow
    units: state
    every: 10s
     crit: $overflow > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Log Service Overflow State
     info: Redfish ${label:resource_name} reports a non-healthy state for Log Service Overflow State
       to: sysadmin

 template: redfish_manager_conditions
       on: redfish.manager.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Manager Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Manager Conditions
       to: sysadmin

 template: redfish_manager_health
       on: redfish.manager.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Manager Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Manager Health
       to: sysadmin

 template: redfish_manager_health_rollup
       on: redfish.manager.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Manager Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Manager Health Rollup
       to: sysadmin

 template: redfish_memory_conditions
       on: redfish.memory.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Memory Module Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Memory Module Conditions
       to: sysadmin

 template: redfish_memory_health
       on: redfish.memory.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Memory Module Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Memory Module Health
       to: sysadmin

 template: redfish_memory_health_rollup
       on: redfish.memory.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Memory Module Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Memory Module Health Rollup
       to: sysadmin

 template: redfish_network_adapter_conditions
       on: redfish.network_adapter.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Network Adapter Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Network Adapter Conditions
       to: sysadmin

 template: redfish_network_adapter_health
       on: redfish.network_adapter.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Network Adapter Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Network Adapter Health
       to: sysadmin

 template: redfish_network_adapter_health_rollup
       on: redfish.network_adapter.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Network Adapter Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Network Adapter Health Rollup
       to: sysadmin

 template: redfish_network_device_function_conditions
       on: redfish.network_device_function.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Network Device Function Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Network Device Function Conditions
       to: sysadmin

 template: redfish_network_device_function_health
       on: redfish.network_device_function.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Network Device Function Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Network Device Function Health
       to: sysadmin

 template: redfish_network_device_function_health_rollup
       on: redfish.network_device_function.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Network Device Function Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Network Device Function Health Rollup
       to: sysadmin

 template: redfish_network_interface_conditions
       on: redfish.network_interface.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Network Interface Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Network Interface Conditions
       to: sysadmin

 template: redfish_network_interface_health
       on: redfish.network_interface.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Network Interface Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Network Interface Health
       to: sysadmin

 template: redfish_network_interface_health_rollup
       on: redfish.network_interface.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Network Interface Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Network Interface Health Rollup
       to: sysadmin

 template: redfish_network_port_conditions
       on: redfish.network_port.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Network Port Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Network Port Conditions
       to: sysadmin

 template: redfish_network_port_health
       on: redfish.network_port.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Network Port Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Network Port Health
       to: sysadmin

 template: redfish_network_port_health_rollup
       on: redfish.network_port.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Network Port Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Network Port Health Rollup
       to: sysadmin

 template: redfish_pcie_device_conditions
       on: redfish.pcie_device.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} PCIe Device Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for PCIe Device Conditions
       to: sysadmin

 template: redfish_pcie_device_health
       on: redfish.pcie_device.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} PCIe Device Health
     info: Redfish ${label:resource_name} reports a non-healthy state for PCIe Device Health
       to: sysadmin

 template: redfish_pcie_device_health_rollup
       on: redfish.pcie_device.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} PCIe Device Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for PCIe Device Health Rollup
       to: sysadmin

 template: redfish_pcie_function_conditions
       on: redfish.pcie_function.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} PCIe Function Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for PCIe Function Conditions
       to: sysadmin

 template: redfish_pcie_function_health
       on: redfish.pcie_function.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} PCIe Function Health
     info: Redfish ${label:resource_name} reports a non-healthy state for PCIe Function Health
       to: sysadmin

 template: redfish_pcie_function_health_rollup
       on: redfish.pcie_function.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} PCIe Function Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for PCIe Function Health Rollup
       to: sysadmin

 template: redfish_port_conditions
       on: redfish.port.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Port Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Port Conditions
       to: sysadmin

 template: redfish_port_health
       on: redfish.port.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Port Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Port Health
       to: sysadmin

 template: redfish_port_health_rollup
       on: redfish.port.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Port Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Port Health Rollup
       to: sysadmin

 template: redfish_power_subsystem_conditions
       on: redfish.power_subsystem.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Power Subsystem Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Power Subsystem Conditions
       to: sysadmin

 template: redfish_power_subsystem_health
       on: redfish.power_subsystem.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Power Subsystem Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Power Subsystem Health
       to: sysadmin

 template: redfish_power_subsystem_health_rollup
       on: redfish.power_subsystem.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Power Subsystem Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Power Subsystem Health Rollup
       to: sysadmin

 template: redfish_power_supply_conditions
       on: redfish.power_supply.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Power Supply Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Power Supply Conditions
       to: sysadmin

 template: redfish_power_supply_health
       on: redfish.power_supply.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Power Supply Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Power Supply Health
       to: sysadmin

 template: redfish_power_supply_health_rollup
       on: redfish.power_supply.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Power Supply Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Power Supply Health Rollup
       to: sysadmin

 template: redfish_power_supply_line_input_status
       on: redfish.power_supply.line_input_status
    class: Errors
     type: System
component: Hardware
     calc: $out_of_range + $loss_of_input
    units: state
    every: 10s
     warn: $out_of_range > 0
     crit: $loss_of_input > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Power Supply Line Input Status
     info: Redfish ${label:resource_name} reports a non-healthy state for Power Supply Line Input Status
       to: sysadmin

 template: redfish_processor_conditions
       on: redfish.processor.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Processor Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Processor Conditions
       to: sysadmin

 template: redfish_processor_health
       on: redfish.processor.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Processor Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Processor Health
       to: sysadmin

 template: redfish_processor_health_rollup
       on: redfish.processor.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Processor Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Processor Health Rollup
       to: sysadmin

 template: redfish_processor_throttling_state
       on: redfish.processor.throttling_state
    class: Errors
     type: System
component: Hardware
     calc: $throttled
    units: state
    every: 10s
     warn: $throttled > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Processor Throttling State
     info: Redfish ${label:resource_name} reports a non-healthy state for Processor Throttling State
       to: sysadmin

 template: redfish_pump_conditions
       on: redfish.pump.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Pump Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Pump Conditions
       to: sysadmin

 template: redfish_pump_health
       on: redfish.pump.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Pump Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Pump Health
       to: sysadmin

 template: redfish_pump_health_rollup
       on: redfish.pump.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Pump Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Pump Health Rollup
       to: sysadmin

 template: redfish_reading_alarm
       on: redfish.reading.alarm
    class: Errors
     type: System
component: Hardware
     calc: $warning + $cap + $alarm + $critical + $emergency + $fault
    units: state
    every: 10s
     warn: $warning > 0 OR $cap > 0 OR $alarm > 0
     crit: $critical > 0 OR $emergency > 0 OR $fault > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} reading ${label:reading_key} Redfish Reading Alarm State
     info: Redfish ${label:resource_name} reading ${label:reading_key} reports a non-healthy state for Redfish Reading Alarm State
       to: sysadmin

 template: redfish_redundancy_conditions
       on: redfish.redundancy.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Redundancy Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Redundancy Conditions
       to: sysadmin

 template: redfish_redundancy_health
       on: redfish.redundancy.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Redundancy Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Redundancy Health
       to: sysadmin

 template: redfish_redundancy_health_rollup
       on: redfish.redundancy.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Redundancy Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Redundancy Health Rollup
       to: sysadmin

 template: redfish_sensor_conditions
       on: redfish.sensor.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Sensor Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Sensor Conditions
       to: sysadmin

 template: redfish_sensor_health
       on: redfish.sensor.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Sensor Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Sensor Health
       to: sysadmin

 template: redfish_sensor_health_rollup
       on: redfish.sensor.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Sensor Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Sensor Health Rollup
       to: sysadmin

 template: redfish_software_conditions
       on: redfish.software.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Software Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Software Conditions
       to: sysadmin

 template: redfish_software_health
       on: redfish.software.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Software Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Software Health
       to: sysadmin

 template: redfish_software_health_rollup
       on: redfish.software.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Software Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Software Health Rollup
       to: sysadmin

 template: redfish_storage_conditions
       on: redfish.storage.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Storage Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Storage Conditions
       to: sysadmin

 template: redfish_storage_health
       on: redfish.storage.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Storage Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Storage Health
       to: sysadmin

 template: redfish_storage_health_rollup
       on: redfish.storage.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Storage Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Storage Health Rollup
       to: sysadmin

 template: redfish_storage_controller_conditions
       on: redfish.storage_controller.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Storage Controller Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Storage Controller Conditions
       to: sysadmin

 template: redfish_storage_controller_health
       on: redfish.storage_controller.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Storage Controller Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Storage Controller Health
       to: sysadmin

 template: redfish_storage_controller_health_rollup
       on: redfish.storage_controller.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Storage Controller Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Storage Controller Health Rollup
       to: sysadmin

 template: redfish_system_conditions
       on: redfish.system.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} System Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for System Conditions
       to: sysadmin

 template: redfish_system_health
       on: redfish.system.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} System Health
     info: Redfish ${label:resource_name} reports a non-healthy state for System Health
       to: sysadmin

 template: redfish_system_health_rollup
       on: redfish.system.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} System Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for System Health Rollup
       to: sysadmin

 template: redfish_thermal_subsystem_conditions
       on: redfish.thermal_subsystem.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Thermal Subsystem Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Thermal Subsystem Conditions
       to: sysadmin

 template: redfish_thermal_subsystem_health
       on: redfish.thermal_subsystem.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Thermal Subsystem Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Thermal Subsystem Health
       to: sysadmin

 template: redfish_thermal_subsystem_health_rollup
       on: redfish.thermal_subsystem.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Thermal Subsystem Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Thermal Subsystem Health Rollup
       to: sysadmin

 template: redfish_volume_conditions
       on: redfish.volume.conditions
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: conditions
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Volume Conditions
     info: Redfish ${label:resource_name} reports a non-healthy state for Volume Conditions
       to: sysadmin

 template: redfish_volume_health
       on: redfish.volume.health
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Volume Health
     info: Redfish ${label:resource_name} reports a non-healthy state for Volume Health
       to: sysadmin

 template: redfish_volume_health_rollup
       on: redfish.volume.health_rollup
    class: Errors
     type: System
component: Hardware
     calc: $warning + $critical
    units: state
    every: 10s
     warn: $warning > 0
     crit: $critical > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Volume Health Rollup
     info: Redfish ${label:resource_name} reports a non-healthy state for Volume Health Rollup
       to: sysadmin

 template: redfish_volume_write_cache_state
       on: redfish.volume.write_cache_state
    class: Errors
     type: System
component: Hardware
     calc: $degraded + $unprotected
    units: state
    every: 10s
     warn: $degraded > 0
     crit: $unprotected > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} Volume Write Cache State
     info: Redfish ${label:resource_name} reports a non-healthy state for Volume Write Cache State
       to: sysadmin

 template: redfish_system_hw_sensor_current_alarm
       on: system.hw.sensor.current.alarm
    class: Errors
     type: System
component: Hardware
chart labels: _collect_module=redfish
     calc: $warning + $cap + $alarm + $critical + $emergency + $fault
    units: state
    every: 10s
     warn: $warning > 0 OR $cap > 0 OR $alarm > 0
     crit: $critical > 0 OR $emergency > 0 OR $fault > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} reading ${label:reading_key} Current Alarm State
     info: Redfish ${label:resource_name} reading ${label:reading_key} reports a non-healthy state for Current Alarm State
       to: sysadmin

 template: redfish_system_hw_sensor_energy_alarm
       on: system.hw.sensor.energy.alarm
    class: Errors
     type: System
component: Hardware
chart labels: _collect_module=redfish
     calc: $warning + $cap + $alarm + $critical + $emergency + $fault
    units: state
    every: 10s
     warn: $warning > 0 OR $cap > 0 OR $alarm > 0
     crit: $critical > 0 OR $emergency > 0 OR $fault > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} reading ${label:reading_key} Energy Alarm State
     info: Redfish ${label:resource_name} reading ${label:reading_key} reports a non-healthy state for Energy Alarm State
       to: sysadmin

 template: redfish_system_hw_sensor_fan_alarm
       on: system.hw.sensor.fan.alarm
    class: Errors
     type: System
component: Hardware
chart labels: _collect_module=redfish
     calc: $warning + $cap + $alarm + $critical + $emergency + $fault
    units: state
    every: 10s
     warn: $warning > 0 OR $cap > 0 OR $alarm > 0
     crit: $critical > 0 OR $emergency > 0 OR $fault > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} reading ${label:reading_key} Rotational Speed Alarm State
     info: Redfish ${label:resource_name} reading ${label:reading_key} reports a non-healthy state for Rotational Speed Alarm State
       to: sysadmin

 template: redfish_system_hw_sensor_humidity_alarm
       on: system.hw.sensor.humidity.alarm
    class: Errors
     type: System
component: Hardware
chart labels: _collect_module=redfish
     calc: $warning + $cap + $alarm + $critical + $emergency + $fault
    units: state
    every: 10s
     warn: $warning > 0 OR $cap > 0 OR $alarm > 0
     crit: $critical > 0 OR $emergency > 0 OR $fault > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} reading ${label:reading_key} Relative Humidity Alarm State
     info: Redfish ${label:resource_name} reading ${label:reading_key} reports a non-healthy state for Relative Humidity Alarm State
       to: sysadmin

 template: redfish_system_hw_sensor_power_alarm
       on: system.hw.sensor.power.alarm
    class: Errors
     type: System
component: Hardware
chart labels: _collect_module=redfish
     calc: $warning + $cap + $alarm + $critical + $emergency + $fault
    units: state
    every: 10s
     warn: $warning > 0 OR $cap > 0 OR $alarm > 0
     crit: $critical > 0 OR $emergency > 0 OR $fault > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} reading ${label:reading_key} Power Alarm State
     info: Redfish ${label:resource_name} reading ${label:reading_key} reports a non-healthy state for Power Alarm State
       to: sysadmin

 template: redfish_system_hw_sensor_pressure_alarm
       on: system.hw.sensor.pressure.alarm
    class: Errors
     type: System
component: Hardware
chart labels: _collect_module=redfish
     calc: $warning + $cap + $alarm + $critical + $emergency + $fault
    units: state
    every: 10s
     warn: $warning > 0 OR $cap > 0 OR $alarm > 0
     crit: $critical > 0 OR $emergency > 0 OR $fault > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} reading ${label:reading_key} Barometric Pressure Alarm State
     info: Redfish ${label:resource_name} reading ${label:reading_key} reports a non-healthy state for Barometric Pressure Alarm State
       to: sysadmin

 template: redfish_system_hw_sensor_temperature_alarm
       on: system.hw.sensor.temperature.alarm
    class: Errors
     type: System
component: Hardware
chart labels: _collect_module=redfish
     calc: $warning + $cap + $alarm + $critical + $emergency + $fault
    units: state
    every: 10s
     warn: $warning > 0 OR $cap > 0 OR $alarm > 0
     crit: $critical > 0 OR $emergency > 0 OR $fault > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} reading ${label:reading_key} Temperature Alarm State
     info: Redfish ${label:resource_name} reading ${label:reading_key} reports a non-healthy state for Temperature Alarm State
       to: sysadmin

 template: redfish_system_hw_sensor_voltage_alarm
       on: system.hw.sensor.voltage.alarm
    class: Errors
     type: System
component: Hardware
chart labels: _collect_module=redfish
     calc: $warning + $cap + $alarm + $critical + $emergency + $fault
    units: state
    every: 10s
     warn: $warning > 0 OR $cap > 0 OR $alarm > 0
     crit: $critical > 0 OR $emergency > 0 OR $fault > 0
    delay: up 5m down 15m multiplier 1.5 max 1h
  summary: Redfish ${label:resource_name} reading ${label:reading_key} Voltage Alarm State
     info: Redfish ${label:resource_name} reading ${label:reading_key} reports a non-healthy state for Voltage Alarm State
       to: sysadmin
