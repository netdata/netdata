import React from "react"

import { Icon } from "components/icon"

interface Props {

}
// eslint-disable-next-line no-empty-pattern
export const ResizeHandler = ({

}: Props) => (
  <div className="netdata-legend-resize-handler">
    <Icon iconType="resize" />
  </div>
)
