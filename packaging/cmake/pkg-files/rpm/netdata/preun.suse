%service_del_preun netdata.service
%service_del_preun netdata-updater.timer

if [ "${1}" -eq 0 ]; then
  if [ -x "/usr/libexec/netdata/netdata-updater.sh" ]; then
    /usr/libexec/netdata/netdata-updater.sh --disable-auto-updates
  fi
fi
